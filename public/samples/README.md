# QA documentation samples

Drop sanitized test plans, bug reports, or checklists here as PDF or Markdown.

Then update `workSamples` in `src/data/portfolioData.js` with the public URL (e.g. `/samples/bug-report.pdf`).
